services:
  my-stock:
    build:
      context: .
      dockerfile: Dockerfile
    image: my-stock:latest
    container_name: my-stock
    ports:
      - "8501:8501"
    volumes:
      - ./data:/app/data
    restart: unless-stopped
