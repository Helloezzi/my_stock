# my_stock

## Local Development
### 1. Install
```powershell
python -m venv .venv
.venv\Scripts\Activate.ps1
pip install -r requirements.txt
```

### 2. Run
```powershell
streamlit run app.py
```

App default URL:
- `http://localhost:8501`
